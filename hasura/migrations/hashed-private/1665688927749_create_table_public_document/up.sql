CREATE TABLE "public"."document" ("cid" text NOT NULL, "owner_actor_id" uuid NOT NULL, "name" text NOT NULL, "description" text NOT NULL, "to_actor_id" UUID, "created_at" Timestamp NOT NULL, PRIMARY KEY ("cid") , FOREIGN KEY ("owner_actor_id") REFERENCES "public"."actor"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("to_actor_id") REFERENCES "public"."actor"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("cid"));COMMENT ON TABLE "public"."document" IS E'Stores owned and shared document metadata';
